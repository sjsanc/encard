version: '3'

tasks:
  run:
    cmds:
      - go run cmd/encard/*.go

  lint:
    cmds:
      - golangci-lint run

  test:
    cmds:
      - go test -v ./...

  build:
    cmds:
      - go build cmd/encard/main.go